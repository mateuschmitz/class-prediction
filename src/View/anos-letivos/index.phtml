<div class="container">

    <div class="row">
        <div class="col-sm-12">
            <p><?php $this->flashSession->output() ; ?></p>
        </div>
        <div class="col-sm-12">
            <h1>Lista de Anos Escolares</h1>
        </div>
    </div>

    <div class="row">
        <div class="col-sm-12">
            <table class="table table-hover table-striped table-bordered text-center">
                <thead">
                    <th class="text-center">Id</th>
                    <th class="text-center">Ano</th>
                    <th class="text-center">Início</th>
                    <th class="text-center">Fim</th>
                    <th class="text-center">Criado Em</th>
                    <th class="text-center">Opções</th>
                </thead>
                <?php
                    $anos = \Application\Model\AnoEscolar::find();
                    if ($anos->count() == 0) {
                        echo "<tr><td colspan='5'>Não há anos letivos cadastrados</td></tr>";
                    }

                    foreach ($anos as $ano) {
                        echo "<tr>
                            <td>{$ano->getId()}</td>
                            <td>{$ano->getAno()}</td>
                            <td>" . (new \DateTime($ano->getInicio()))->format('d/m/Y') . "</td>
                            <td>" . (new \DateTime($ano->getFim()))->format('d/m/Y') . "</td>
                            <td>" . (new \DateTime($ano->getCreated()))->format('d/m/Y H:i:s') . "</td>
                            <td><a class='btn btn-default' href='/anos-letivos/{$ano->getId()}/edit' role='button'>Editar</a></td>
                        </tr>";
                    }
                ?>
            </table>
        </div>
    </div>
</div>